import { useContactStore } from "./contacts.slice";
import { useUserStore } from "@/store/slices/user.slice";
import { useGlobalStore } from "@/store/slices/global.slice";

export { useGlobalStore, useUserStore, useContactStore };
